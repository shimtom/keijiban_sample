header
    nav(class="navbar navbar-default navbar-fixed-top")
        .container-fluid
            .navbar-header
                a(class="navbar-brand" href="/") Bulletin Board
            .collapse.navbar-collapse
                ul.nav.navbar-nav.navbar-right
                    if username
                        li.dropdown
                            a(href='' data-toggle='dropdown' role='button' aria-haspopup='true' aria-expanded='false').dropdown-toggle Signed in as
                                strong  #{ username }
                                =' '
                                span.caret
                            ul.dropdown-menu
                                li
                                    a(href='/logout') Sign out
